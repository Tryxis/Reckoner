{
  "name": "reckoner",
  "version": "1.0.0",
  "description": "Attack sequence probability calculator for tabletop wargames.",
  "author": "David Butterworth",
  "license": "MIT",
  "private": true,
  "scripts": {
    "serve": "npx --yes serve www -l 5173",
    "icons": "python3 tools/make-icons.py",
    "sync": "cap sync",
    "ios": "cap sync ios && cap open ios",
    "android": "cap sync android && cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  }
}
